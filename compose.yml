services:
  wallet-web:
    image: python:3.11-slim 
    container_name: bitcoin-wallet-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./:/wallet
    command:
      - /wallet/start.sh
